//
// SEARCH
//
// searches google for the pages associated with the target

var request = require('request');

module.exports = search;

function search (state, callback) {
  
  // TODO: make the request to google
  
  // TODO: stash the urls in state and pass them along
  
  return callback(null, state)
};